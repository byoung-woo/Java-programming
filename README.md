# ☕ Java Programming

자바 프로그래밍 언어의 기초부터 객체 지향 프로그래밍, GUI 구현 및 파일 입출력까지의 학습 과정과 시험 문제 풀이 코드를 정리한 저장소입니다.

---

## 📂 디렉토리 구조 및 주요 내용

### 1) 중간고사 실습 (`1_middle/`)
자바의 기초 문법, 클래스 상속, 그리고 기초 AWT GUI 활용을 다룹니다.

- **Calculator.java**: AWT `Frame`과 `GridLayout`을 활용한 기본 계산기 UI 구현  
- **Canvas.java**: `MyCanvas` 클래스를 정의하여 그래픽(원 그리기) 출력 실습  
- **Student.java**: 클래스 상속(`Member → Student`)과 생성자, 정보 출력 로직 구현  

---

### 2) 기말고사 실습 (`2_final/`)
Swing 라이브러리를 이용한 심화 GUI와 자바 입출력(NIO/IO)을 다룹니다.

- **Pro1.java**: `JProgressBar`로 실시간 투표율을 시각화하는 투표 시스템 구현  
- **Pro2.java**: `StringTokenizer`로 텍스트 영역의 문자/단어/라인 수를 계산하는 유틸리티  
- **Pro3.java**: `FileChannel`과 `ByteBuffer`로 여러 PPM 이미지 파일을 하나로 합성하는 파일 처리 알고리즘  

---

### 3) 자바 프로젝트 (`3_project/`)
- **ImageFilterApp.java**: 이미지를 사분면으로 분할해 구역별로 서로 다른 필터(흑백/세피아/반전 등)를 적용하고 관리하는 종합 GUI 애플리케이션

> 상세 내용은 해당 폴더의 `README.md`에서 확인할 수 있습니다.

---

## 🛠️ 개발 및 실행 환경
- **Language**: Java 8 이상  
- **GUI Library**: AWT, Swing  
- **I/O**: Java NIO (Channel, Buffer)

---

## 🚀 실행 방법 (예시)

```bash
# 컴파일
javac 1_middle/Calculator.java

# 실행
java 1_middle.Calculator
```

> 패키지 선언 여부에 따라 실행 커맨드는 달라질 수 있습니다.  
> (예: `package lab;`가 있다면 `java lab.Calculator` 형태로 실행)
